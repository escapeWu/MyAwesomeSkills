interface:
  display_name: "External Collaboration Workflow"
  short_description: "Lightweight external proposal handoff"
  default_prompt: "Use $external-collaboration-workflow to preserve an external-team source, record concise adoption decisions, and route accepted work through the normal lightweight project workflow."
